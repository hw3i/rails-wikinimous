
  <%= form_for(@article, :html => {class: "form-something" } ) do |a| %>
    <%= a.label :title %>
    <%= a.text_field :title %>
    <%= a.label :content %>
    <%= a.text_area :content, id: "content" %>


    <%= a.submit :class => "btn btn-primary" %>
  <% end %>

